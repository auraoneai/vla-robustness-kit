# Mock Policy

The bundled mock policy is deterministic and intentionally simple. It fails on
dim lighting, shifted cameras, metadata mismatches, missing coordinate frames,
and task-phase reorders so reports have stable failure clusters.

